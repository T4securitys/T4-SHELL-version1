#!bin/bash
if [ "$`shell -h`" = "-r" ]; then
  n=""
  c="\c"
else
  r="-r"
  c=""
fi

echo $h (1)ap1 -r = refresh root
        (2)afct = activate fake clone tree
        (3)adfct = deactivate fake clone tree
        (4)a1sa-reppo = show activated respiratory
        (5)bo19 = show a animated logo
        (6)install <package name> = Install a package
        (7)pip <install<package name> = install a package using python
        (8)PKG <install<package name> = install a package using termux/linux shell
       +==========================================================================+
             Many commands will come on "T4-SHELL-version2"
       +==========================================================================+:  
       
exit 1

if [ "$'pkg install ap1" ]; then
fi
echo $ checking commands...Done!
       waiting for download...Done!
       waiting for your device permission...Done!
       Installing...
    1% |-█
    10%|-██
    20%|-███
    30%|-████
    50%|-█████
    90%|-██████
   100%|-███████
       |─────────|
           Done
 INSTALLED!!! :

